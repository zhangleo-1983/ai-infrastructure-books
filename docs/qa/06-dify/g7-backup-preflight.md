# 第六册 G7 备份前实际挂载核对

最新状态：2026-09-11 21:01 已完成 G7.1—G7.8 的服务器侧步骤。十二材料在 VPS 与电脑全部校验通过；恢复副本同版本启动、六卷/文件/数据库核对、internal 网络与外连阻断通过；恢复副本已停止，原栈已恢复，受保护 route 与 JWT 已恢复。恢复副本管理员网页登录仍待 Owner 输入原密码后完成，见 [暂停交接](pause-checkpoint.md)。下文保留准备时点记录。

2026-09-11 只读盘点与差异修正；尚未制作冷备份、启动恢复副本或证明恢复通过。

- 原栈 16 个容器，12 个唯一运行镜像；本机镜像总占用约 11.23 GB。VPS 根分区约 120 GB 可用；受控电脑约 57 GiB 可用，FileVault 显示 On。
- 全部 bind 路径在本项目 docker 目录，另有 **6 个实际 Docker 卷**：2 个 Agent named volume，以及 ssrf_proxy / agent_ssrf_proxy 各自的 /var/log/squid、/var/spool/squid 四个匿名卷。
- 实际 Squid image 的 Config.Volumes 声明两个路径；因此只读 Compose 顶层 volumes 会漏掉四个运行时卷。实际卷映射保留受控环境，公开记录不写自动卷名。
- 校订第 11/12 章、附录、事实表与 runbook。恢复覆盖文件显式声明四个代理匿名卷且 volume.nocopy=true；恢复检查器要求四个路径齐全。新增 proxy-volumes.py，只有全栈停止且四卷独立、没有被其他容器共享时才归档；恢复先核原/新卷不同、四目标全空，再使用已有 init_permissions 镜像的断网只读工具容器回填，不拉镜像、不覆盖归档、不自动创建缺失卷。
- 七份原材料扩为十二份：增加四个代理 tar 和 proxy-volumes.json。原 docker 目录和两份 Agent 卷包仍完整保留，没有因为加入代理卷而缩减范围。
- 25 项 Python 离线回归和完整 npm run check 通过；覆盖原项目拒绝、共享/缺失/运行中卷拒绝、nocopy、原目录与网络隔离。脚本/覆盖文件已复制到 VPS，尚未执行备份动作。

后续仍须正常停写、逐包 SHA256、VPS 外副本、真正空目录与六卷恢复、实际网络隔离、管理员/资料/历史及经济检索读写核验；静态检查不能替代这些步骤。

## G5 等待本人登录期间的镜像准备

- 原项目保持运行，仅保存恢复用固定镜像：16 项服务、12 个不同 image ID；源码仍为 1.17.1 和原提交。
- VPS 项目外新建 BACKUP-A 子目录（700），保存 image-map.txt、compose.images.yaml、version.txt、images.tar。镜像包 2710451200 字节。
- 镜像包已传输到本机 FileVault 开启的受控目录，目录 700、文件 600；源和本机 SHA-256 一致。
- 这只是预先保存镜像，不是完整停写备份。数据库、目录、六个卷及十二材料总清单尚未归档或恢复；原服务未因此停机。
