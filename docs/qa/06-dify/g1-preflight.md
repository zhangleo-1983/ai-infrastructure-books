# G1 盘点与执行前准备

核对日期：2026-09-11（Asia/Shanghai）。Owner 回复“开始吧”，授权开始上一轮约定的只读资源与报价核对；
并自行完成 Vultr 重新登录。随后回复“OK，继续”，确认单台配置及 3 美元/最长 24 小时边界。
Owner 再回复“给你完全授权，持续推进，G1-G8”。已登记本册公钥、创建专用防火墙与唯一 VPS；
SSH、Docker 和入站限制已验证。Tunnel 表单已准备，新增连接凭证的动作确认待回复；没有模型请求。
本页记录初始基线及本轮实际执行，支持[结果台账](field-validation-results.md)；G1 尚未整门通过。

## 创建前观察的账户基线（历史快照）

| 证据代号 | 页面与实际结果 | 限制与处理 |
| --- | --- | --- |
| G1-P01 | Vultr 登录后 Instances 显示 No Instances | 只证明实例清单为空，不推断快照、磁盘、余额或整个账户零费用 |
| G1-P02 | Cloudflare 刷新后的 Tunnel 页为空清单/开始使用页面 | 当前没有列出的 Tunnel；不由此推断所有其他产品或 Access 应用为空 |
| G1-P03 | 原教学 zone 为 Free、完整 DNS；记录表共 1 条 DNS-only CNAME，指向 Vercel | 该记录属于既有其他项目，必须保留。原域名不能再称整域空闲；拟用独立 dify 子域，未创建 |
| G1-P04 | DNS 设置显示 DNSSEC 保护成功、已分配两台 Cloudflare nameserver | 本次未改 NS、DNSSEC、DS 或 DNS；未重新验证注册商控制权、公开解析器与续费日期 |
| G1-P05 | zone 的 Access 页面为产品介绍/开始使用 | 不能据此认定 Zero Trust 已开通或账户应用为空；套餐和应用实际状态留待后续核对 |
| G1-P07 | Billing 显示本月待结算 0.24 美元、已出账应付 0.00 美元、账户余额 0.00 美元 | 0.24 是本轮创建前基线；历史促销额度已过期，不作为本轮可用余额。税率尚未核定 |
| G1-P08 | 当前带宽页显示 2.05 TB 池、用量四舍五入为 0.00 TB、超额 0.00 TB、超额费 0.00 美元 | 是账户汇总，不等于精确零用量；不归算为本册使用 |
| G1-P09 | 本机生成本册独立 Ed25519 密钥，私钥权限 0600；公钥登记页名称已填写 | 私钥和当前出口地址仅在本机受控目录。公钥尚未输入或提交；SSH 实际出口与连接待核 |
| G1-P06 | Vultr 有一个历史防火墙组，1 条规则、0 个关联实例；SSH 下拉有一项历史公钥 | 均不选择、不改动。本机 .ssh 顶层公钥清单未找到同名教学公钥，不能声称历史密钥可用；拟建本册专用 key 与防火墙 |

真实域名、账号、对象 ID 和规则来源地址只在当前受控控制台核对，仓库使用代号。
G1-P03 的非本册记录必须加入后续前后对照与 G8 保留清单。

## 报价与表单核对

[Vultr 官方公开价](https://www.vultr.com/pricing/#cloud-compute)的 Regular Performance 为
4 vCPU、8 GB、160 GB SSD、4 TB/月标称流量、40 美元/月、0.060 美元/小时。
登录后的新旧创建表单均显示同规格 **40 美元/月、0.055 美元/小时**。
这是当次实际显示差异，未证明最终账单采用哪个小时值；本次测算使用较高的 0.060，不自行归因为税或折扣。

Beta 表单在大阪选中后仍提示地区不可用，同时允许进入下一步并启用部署按钮，因此没有提交。
转入其提供的官方旧版创建页，选定下表配置。登记公钥后重开表单，发现自动备份和 IPv6 恢复勾选；
提交前再次取消并核对总价、单台数量及专用公钥/防火墙。唯一一次 Deploy 返回创建成功。

| 字段 | 已填写或待完成 |
| --- | --- |
| 账户/用途 | 当前 Vultr 个人账户；本册专用、无真实业务 |
| 数量/名称 | 1 台；实际标签与主机名 `book06-dify-lab` |
| 地区/套餐 | Osaka / Regular Cloud Compute，`vc2-4c-8gb` |
| 系统 | Ubuntu 24.04 LTS x64，已核对实际选中值 |
| CPU/内存/系统盘 | 4 vCPU / 8 GB / 160 GB SSD |
| 网络 | Public IPv4；IPv6 不选；无 VPC / NAT Gateway |
| 附加付费项 | 自动备份与付费 DDoS 均不选；不加磁盘、快照、对象存储或负载均衡 |
| 凭证/防火墙 | 已绑定本册专用公钥与新组；唯一 Accept SSH 22 规则来源为管理出口 /32，其他默认 Drop；实例设置页确认关联 |
| 当前表单总价 | 40 美元/月（0.055 美元/小时）；创建后实例 Current Charges 显示 0.06 美元 |

SSH 私钥仅存本机受控目录；只把公钥上传到本人的 Vultr 账户。不得为方便复用 GitHub 私钥。
新防火墙拒绝其他公网入站；Docker 与 cloudflared 使用所需出站，不开放 80、443、8088 或插件端口。
若管理出口变化，先核对新地址与连接路径，不自动放宽为任意来源。

## 已确认的费用与期限

- 本次只创建上表 1 台实例：**Vultr 累计预算 3 美元，创建成功后最长 24 小时**，以先触及者为停止边界。
- 按较高公开小时价，24 小时基础费为 `0.060 × 24 = 1.44` 美元；剩余预算留给实际税费和不可忽略的计费差异，不用于升级或附加产品。
- 账单与带宽基线已核，见 G1-P07/P08；0.24 美元既有费用不计入本轮新增预算。创建后实例费显示 0.06 美元；税率未单列，最终税费与结算仍须 G8 核对，不宣称免税。全部新增费用受 3 美元范围约束。
- 控制台月价是持续持有的标价，不代表本次承诺运行整月。账单按小时累积，最小单位一小时；停止实例仍计费，销毁才停止实例计费。[官方计费说明](https://docs.vultr.com/support/platform/billing/how-am-i-billed-for-my-servers)
- 标称月流量不是刚创建就拿到全部额度。实际分配按小时累计，须看账户当前可用额；下载镜像与取回备份的出入方向分别核对。[官方流量计算](https://docs.vultr.com/support/platform/billing/how-are-bandwidth-caps-calculated)
- 自动备份通常增加 20% 基础费，本候选已取消该附加项，G7 仍需完整冷备份。[官方备份说明](https://docs.vultr.com/vps-automatic-backups)
- Cloudflare 公共价格页列有 0 美元的 Zero Trust Free，适用于小于 50 用户的团队；本轮仅本人使用；Zero Trust 概述页已确认现有组织为 Free，无需本轮新增套餐或支付方式。[官方套餐](https://www.cloudflare.com/plans/)
- Owner 的 G1—G8 全程授权采用已展示方案的 4 元停发线、5 元上限、初始最多 12 次模型请求；G4 前仍核实际价格和用量，G1 没有模型请求。

实例创建后立即记录绝对清理时刻（创建成功时间加 24 小时）与负责人；预计到期前进入清理。
预算和时限是人工操作约束，不是提供方硬限额。当前没有定时销毁任务，也没有承诺后台自动清理。
若会话结束或测试停滞，Owner 需按受控清单及时销毁实例，不能把关机当止费；需延长时先明确新范围。

## G1.2—G1.4 实际执行与当前停止点

| 证据 | 实际结果 |
| --- | --- |
| G1-R01 | 2026-09-11T08:51:19Z 唯一 VPS 创建成功；北京时间最迟清理为 2026-09-12 16:51:19。实例表单 Created 日期显示与本机 UTC 不同，采用本机可信时钟记录动作时刻，不推断其显示时区 |
| G1-R02 | 提供方 noVNC 读取 Ed25519 主机指纹，与 SSH 扫描一致；专用 known_hosts 严格校验，两次独立 SSH 成功；实际 SSH 来源等于批准的 /32 |
| G1-R03 | Ubuntu 24.04.4 LTS、x86_64、4 CPU、约 7935 MiB 内存；根盘可用约 132 GiB，cloud-init done；只记录当前空载情况，不当作 Dify 性能结论 |
| G1-R04 | Docker 官方 Noble APT 安装：Engine 29.8.0、Compose v5.5.1，systemd active/enabled；hello-world 从 Docker Hub 下载并正常运行后自动移除容器 |
| G1-R05 | 主机 UFW 仅允许管理出口 /32 的 TCP 22；删除新机默认的任意来源 IPv4/IPv6 SSH 规则。sshd 密码和键盘交互认证关闭，root 仅公钥；配置校验和收紧后的独立 SSH 均通过 |
| G1-R06 | 服务器公网监听仅 SSH；80/443/8088/8089/5001/5002 的实际 HTTP 探测均超时、无 HTTP 响应。原始 TCP connect 在本机代理下全部提前返回成功，已弃用为开放判断依据；第二独立网络仍待 G2/G5 |
| G1-R07 | Cloudflare 官方 Noble APT 安装 cloudflared 2026.9.0；尚未安装连接凭证或启动 connector。现有 Zero Trust 组织为 Free；Tunnel 创建页名称已填写，未提交 |

控制台第一次登录因键盘大小写处理失败；改用显式 Shift 组合后成功，未重置凭证或重装主机。
以上恢复后才继续 SSH。表单默认项重置与代理探测差异应作为教学校订候选，尚未改动已验收正文。

Owner 随后回复“确认，需要什么直接弄，快速弄完”，Tunnel 凭证动作确认已取得。
G1-R08：2026-09-11 17:16—17:18+08:00 创建并连接唯一 Tunnel，列表为健康、1 个副本、2026.9.0、无路由。
凭证保存在 VPS root 专属 600 文件，systemd 读取 token-file；active/enabled，重启恢复。
cloudflared 自检 DNS、两组 UDP QUIC、TCP HTTP/2 与 Cloudflare API 均通过；未创建 route 或 DNS。
G1 已通过，连续进入 G2；书籍正式发布、Git 提交/推送不在本轮实机授权内。

VPS、专用云防火墙/公钥、本机私钥、known_hosts、SSH 配置与对象映射均列入本轮保留/清理账。
VPS 持续计费；绝对期限见 G1-R01。没有自动销毁任务。Owner 保管受控清单，Codex 在活动会话继续执行；
若离开或暂停，须在期限前清理或明确调整范围，不能把停止实例视为止费。既有 Vercel CNAME、根 zone、NS 与 DNSSEC 保留。

## 本地记录检查

仅修改 QA、规划与项目状态；检查本地链接、表格步骤、敏感模式、空白、diff 和状态 Schema。
本次未修改正文/代码，没有重复运行页面布局、打印或整套代码测试；准备阶段的测试仍是历史记录。
