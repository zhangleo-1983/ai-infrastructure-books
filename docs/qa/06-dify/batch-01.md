# 第六册 P3 第一批验收

日期：2026-09-11。Owner 已回复“没问题，通过验收，继续写”，确认 P2 三篇样章。
本批结果：第 1—6 章及练习材料已写成 draft，本地验证通过，停在本批审阅。
书籍仍为 drafting / 0.0.0；G1—G8 未授权、未执行。

## 交付与原因

补齐六篇前置正文，使已确认第 7 章工作流样章有明确的部署、模型、输入与知识库交接：

| 章节 | 交付重点 |
| --- | --- |
| 1 从聊天到工作流 | 任务卡、三个基本节点、变量与连线、单次 Workflow、检索与生成的边界 |
| 2 规划服务与账单 | 全新隔离环境、资源与费用卡、16 个服务的职责、回环端口、持久数据归属 |
| 3 启动 Dify | 固定 1.17.1 官方文件、随机配套秘密、端口差异、有效配置检查、镜像记录、运行状态与 SSH 转发 |
| 4 工作区与模型 | 唯一 Owner、安装/调试/更新权限、官方插件身份、Flash 名称衔接、保存凭证前核账 |
| 5 第一次工作流 | question 必填短文本、200 字符、非思考与 256 输出预算、变量选择、answer、一次合成运行与草稿保存 |
| 6 建立知识库 | 三份纯文本保存、手工导入、General 分段、经济索引、四题检索证据与交接卡 |

新增材料存于 `src/content/book-assets/06-dify/`，由 MDX 直接 raw 导入：
`prepare-env.py`、`check-config.py`、`compose.book06.yaml`、三份合成 TXT。
没有放到 public 目录，避免草稿文件绕过生产排除。脚本可见、可复制、可独立测试；不接收模型凭证，
不启动容器，随机秘密只在本机内部处理。新增 `tests/book06-config-test.py` 验证这些可执行边界。

三份 TXT 的 R01—R03、S01—S03、C01—C03 均逐段复核，人数、日期、费用与取消条件一致。
“交通补贴”是有关键词却缺少金额的例子，不能把它当作必定空检索样本。

已确认样章只作连续性修改：开始之前更新九篇草稿范围；第 7 章更新前置状态与文件/段号；
第 9 章同步前置状态，并把两个 CORS 来源由 localhost 切换到同一 HTTPS 主机名。
完整修改原因见 [内容进度与逐段复核](content-progress.md)。前五册正文、原型和第二册迁移台账未改。
P2 已有通用完成资格修正和注册表改动保留，本批未再扩展通用交互。

## 检查与实际结果

| 检查 | 本次结果 |
| --- | --- |
| `npm run check` | 通过：typecheck 91 文件、0 错误/警告/提示；lint；175 单元测试；build；系列及前五册内容检查；内部链接 |
| 最终 MDX 调整 | 再次 typecheck、lint、build、links、release 均通过 |
| Python 回归 | `PYTHONDONTWRITEBYTECODE=1 python3 tests/book06-config-test.py`：11 项通过；覆盖秘密配对、600 权限、输出不泄漏、旧配置/数据/悬空链接、模板缺失/重复字段、公网/调试端口、服务集合与特权模式 |
| 官方配置集成 | 临时目录使用官方 1.17.1 Compose、环境模板和仓库内脚本；本地 Compose 5.1.2 仅执行 config；16 服务、唯一 nginx 127.0.0.1:8088 → 80/tcp；无 Docker daemon、pull 或 up |
| 命令语法 | 17 个导出 Shell 命令块在保留示例替换占位符后通过 bash -n；未执行教程中的安装、部署或公网探测 |
| 根路径/子路径构建 | 均为 88 HTML、Pagefind 75 页；链接、release 检查通过；子路径为 /ai-infrastructure-books/ |
| 生产边界 | 5 本可阅读、75 正文、5 正式打印页；2,604 内链通过；第六册正文、脚本与合成资料不在 dist；第二册严格原型检查通过 |
| 草稿页面 | 九篇均无 Pagefind 正文标记、完成资格为 false、无完成复选框；目录显示 9 篇样章；无重复 ID |
| 响应式与深色 | 九篇 × 360/390/768/1440px × 浅/深色，72 组通过，无页面级横向溢出；手机与关键图示人工查看 |
| 复制与本地链接 | 30 个代码块实际复制等于可见文本；179 次本地页面/锚点请求通过 |
| 无 JavaScript | 九篇正文可读，复制按钮隐藏，没有误露完成控制 |
| 无障碍 | 九篇 main 范围 WCAG A/AA 自动检查均 0 违规；未宣称自动检查覆盖全部可访问性 |
| 草稿打印 | 九篇、最终 56 页 A4；noindex、无重复 ID、无空白页、无原始 Markdown 星号；长代码可跨页，三份短 TXT 标题与正文保持一起 |
| 打印人工检查 | 初版 55 页全页缩略图复核；调整 TXT 分页、补工具安装与第二网络命令后生成 56 页，重点复核脚本、端口命令与三份资料页，未见内容裁切或重叠 |
| 官方来源 | 34 个唯一正文官方链接均 HTTP 200；事实与源码阅读结论见 [事实校订](fact-check.md) |
| 敏感模式 | 本册正文、材料、测试与文档未发现真实 key、UUID、JWT 或私钥模式；地址为回环/公开文档/占位符/保留示例，生成的临时秘密未写入仓库 |
| Chromium 生产回归 | 52/52 通过，含前五册导航、响应式、深色、打印、无障碍与中文搜索；在稳定根路径产物上独立运行 |
| 状态与差异 | PROJECT_STATUS 按 P3 证据最小更新，Schema 与 diff 检查通过；没有提交、推送或发布 |

本地临时 QA 目录包含结果 JSON、截图、PDF 与日志，不作为正式出版附件。依赖清单与锁文件未改。
无障碍检查最初因测试器缺少显式 browser context 未执行成功，修正测试器后九篇重新扫描通过；不是产品页面修复。
正式 Firefox/WebKit 和全册 PDF 验收留到完整草稿与 RC 阶段。

## 剩余边界与下一步

- Dify 实机安装、插件包身份、参数兼容、请求与账单、中文检索、真实空结果、Access 全路径、手机与恢复均未执行。
- 官方部分基础镜像使用滚动标签；实际 digest 尚未冻结。第三章是待 G2 校订的草稿，不能宣称完全可复现或已经安全部署。
- 第 5 章前端与服务器输入限制、纯空格、底层重试，以及第 6 章真实分段与关键词命中仍需实机证据。
- 第 8、10—12 章、附录和资料来源尚未创建。按已确认分批约定，本批验收后再写第 8、10—11 章，并复核第 7、9 章衔接。
- 本轮没有操作真实 Dify、Cloudflare 或模型后台，没有模型请求、费用、外部资源变更或发布。
