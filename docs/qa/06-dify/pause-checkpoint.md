# 第六册暂停交接：G8 清理完成

2026-09-11。Owner 已确认继续。G7.4—G7.8 服务器侧核验与 G8 专用资源清理已完成。

## 已完成的小节点

- G1—G5 已通过。Owner 确认桌面登录和真实手机验证；专用 key 最后核账为 8 次、1665 tokens、少于 0.01 元，余 4 次实际提供方请求额度。
- G6 错误路由、API 暂停、错误模型与变量/空分支证据已完成并恢复。worker/插件服务停止和恢复已验证；具体后台任务失败、429、提供方超时仍未覆盖，见 [G6](g6-fault-drills.md)。
- G7.1—G7.3 已完成：固定镜像，盘点全部目录和六个实际卷，正常停写，十二份材料与 SHA256SUMS 齐全。VPS 端与已下载电脑副本全部校验通过。
- 完整 docker.tar.gz 为 25729277 字节；images.tar 为 2710451200 字节。镜像包预先下载，其余材料本次补齐；本机 FileVault 开启，目录 700、文件 600。
- 备份前保存 12 张相关表的计数和 SHA-256：1 管理员、3 应用、4 workflow 版本、3 文档、12 分段、1 知识库、9 条持久运行、6 个上传对象，以及提供方/权限/更新设置。仅在受控目录保存数据指纹，不把凭证或原始记录写进 Git。

## 当前安全状态

- 原项目 16 个容器全部停止，无 exit 137：sandbox 与 web 为 143，其余为 0。PostgreSQL 日志明确 shut down；Redis 最终 RDB 保存并退出；Weaviate 完成 drain/shutdown。
- 原 restart policy 已恢复；容器仍停止。没有启动恢复项目：恢复容器数为 0，恢复目录和六个新卷尚未创建。
- 临时公开 route 已撤，connector 实际 ingress 仅剩 http_status:404；Access、教学 DNS、专用 Tunnel 与 connector 保留待后续恢复/退役。
- 专用模型 key、VPS、原数据、备份和 SSH 文件全部保留。VPS 仍计费，19:28 控制台显示本轮实例 0.18 美元，非最终账单。
- 已授权上限仍为 3 美元/创建后 24 小时，最迟 **2026-09-12 16:51:19+08:00** 清理。没有设置自动续跑或自动销毁。
- 15 个 MDX 继续 draft，书籍 drafting / 0.0.0；未提交、推送或发布站点。

## 本次停机差异，不能遗漏

首次直接 compose stop --timeout 120 导致 nginx、两代理、sandbox 超时退出 137；检测阻止归档，没有用失败状态制作备份。原栈恢复健康后，检查入口脚本，发现 shell 包装进程未按预期转发退出信号。

第二次先临时关闭这四个容器的自动重启，再分别调用 nginx -s quit、squid -k shutdown 和 sandbox main 的 TERM；四者退出 0/143，随后 compose stop 停其余服务，最后恢复 always。全栈正常停止后才归档。原配置、镜像与数据没有为绕过检查而删除或替换。

该流程已整理为新增 stop-stack.py，28 项 Python 回归通过。**目前只逐步实测了等价命令，脚本整体尚未在恢复栈执行；第 11/12 章尚未接入此脚本。** 恢复期间先核验脚本和适用路径，再把实际差异补进正文，保留首次失败。

## 到家确认后的续接顺序

以下顺序已在本次恢复后执行完毕；本节保留为交接历史。

1. 先核当前时间、VPS 费用、SSH 主机指纹、原栈仍停止、route 仍撤；核本机十二材料 SHA256SUMS，不重新制作或覆盖本次备份。
2. 从 **G7.4** 开始：新建 dify-restore/docker，只从此备份恢复；只改恢复环境公开 URL 为 localhost:8089，保留秘密；四文件配置检查，四个 internal 网络和六个全新 nocopy 卷。
3. 仅 create，不启动；原栈继续停止。回填两个 Agent 卷与四个代理卷，核实际镜像、挂载、网络与外连隔离，再启动恢复栈。
4. 开 SSH 8089，准备页面后让 Owner 用原密码登录（不在聊天提供密码）；核管理员/草稿/发布/历史/文档/索引与设置，做经济检索及恢复副本中的停用/启用，不调用模型。留意 G4 已记录的 600 秒临时索引锁，不手动清 Redis。
5. 已完成：恢复栈停止、原栈启动、受保护 route 恢复；本地 setup 200、匿名入口 302，Tunnel 日志确认 JWT required=true、team 与 AUD 正确。整合 stop-stack.py 和实机差异，运行相称检查。
6. G8 已执行：主机名 route 已确认为空，cloudflared 服务已停，VPS 实例已销毁，原/恢复 Docker 资源、项目目录、VPS 备份和本机专用备份/SSH 文件已清理；专用模型 key 已撤销。Cloudflare 专用 Tunnel 仍在控制台显示“已关闭”，因页面操作菜单在当前会话中无法稳定提交删除，已作为明确待办记录；根 zone/NS/DNSSEC 与既有业务对象未改动。

真实对象 ID、专用密钥路径和本机备份位置仍保存在 LEDGER-A 与受控 artifacts 目录，不能复制进公开正文。Vultr 在本次暂停前已由 Owner 重新登录；若会话到期，只在需要时重新交接登录。
