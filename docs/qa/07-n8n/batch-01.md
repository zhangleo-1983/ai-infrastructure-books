# 第七册第一批写作验收

日期：2026-09-11

本批新增第 1—5 章：认识 n8n、触发器与节点、规划自托管、官方 Compose 部署、工作区初始化。三篇样章保持不变，全部新内容仍为 `draft: true`。

已检查：

- 章节 order、slug、book、chapterType、updatedAt 与第七册规格一致；
- 每个操作均说明执行环境、目的、成功状态和失败判断；
- 贯穿案例仍只使用合成报名数据，不创建真实第三方凭证；
- Docker、加密密钥、Webhook 与执行记录边界未提前扩展为生产能力；
- `npm run check`、第七册样章检查与 `git diff --check` 通过。

本批未完成：第 6—12 章、附录、资料来源、外部实机、生产构建、搜索和正式打印。待 Owner 验收后再继续下一批。
