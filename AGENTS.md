## 项目定位

本仓库用于制作《AI 基础设施从零开始》中文在线图书系列。

本项目不是普通博客、营销网站或开发者 API 文档。
它是面向完全零基础用户的图解式技术学习产品。

## 当前内容优先级

当前优先维护：

《拥有自己的海外网络：从 VPS 到 Clash，只需要 30 分钟》

在第二册达到稳定版本前，不得擅自开始开发第三册正文。

## 目标读者

默认读者不具备以下知识：

- Linux
- SSH
- VPS 运维
- 网络协议
- Docker
- 前端开发
- 代理客户端配置

任何步骤不得依赖读者“自行理解”或“自行补充”。

## 写作原则

1. 使用简体中文。
2. 每个专业词第一次出现时解释。
3. 每个操作说明：
   - 做什么
   - 为什么
   - 成功状态
   - 失败判断
4. 不使用“显然”“简单执行即可”“自行处理”等跳步语言。
5. 不为了缩短篇幅牺牲新手可理解性。
6. 保留用户已经确认的正文，除非任务明确要求改写。
7. 技术事实变化时，优先依据官方文档更新。
8. 涉及版本变化的内容注明校订日期。

## 视觉原则

1. 风格清晰、克制、接近高质量官方文档。
2. 中文长文优先保证可读性。
3. 手机端是一级使用场景。
4. 不使用真实软件截图。
5. 软件界面使用 HTML、CSS 或内联 SVG 仿真。
6. 仿真界面不能声称是像素级真实截图。
7. 不引入外部字体文件。
8. 不依赖外部图片才能完成阅读。
9. 深色模式必须完整可用。
10. 打印样式必须保持正文和关键图示可读。

## 内容安全边界

1. 不提供共享 Apple ID。
2. 不提供破解软件、盗版软件或非官方安装包。
3. 不提供绕过付费或平台安全限制的方法。
4. 不承诺匿名性或不可追踪性。
5. 提醒用户遵守所在地法律、服务条款和组织安全制度。
6. 密码、UUID、订阅 URL、私钥均视为敏感凭证。

## 技术原则

1. 优先使用静态站点架构。
2. 保持依赖精简。
3. 组件必须可供后续各册复用。
4. 正文和界面组件分离。
5. 设计 Token 集中管理。
6. 所有页面必须支持响应式布局。
7. 代码块必须支持复制。
8. 章节完成状态可以使用 localStorage。
9. 搜索应在静态部署环境中正常工作。
10. 所有交互在 JavaScript 失效时，正文仍应可以阅读。

## 修改规则

1. 修改前先阅读相关文件。
2. 不覆盖原型文件和历史版本。
3. 大范围重构前先给出计划。
4. 每次任务只处理一个明确结果。
5. 完成后检查 diff，避免无关格式化。
6. 不擅自删除章节、段落、提示框或故障排查内容。
7. 不擅自更换书名、章节顺序和技术路线。

## 验证要求

每次涉及代码修改，应根据改动运行适用的检查：

- build
- typecheck
- lint
- tests
- internal link check
- responsive layout check
- dark mode check
- print preview check

完成报告必须说明：

- 修改了什么
- 为什么修改
- 运行了哪些检查
- 检查结果
- 尚存风险或待办事项

## 里程碑规则

当前工程重构分为五个里程碑：

1. 工程骨架
2. 核心组件与“开始之前”、第 1—2 章试迁移
3. 第 3—12 章、附录和资料来源迁移
4. 交互与搜索
5. 完整验收

每个里程碑完成后必须先汇报并等待项目 Owner 确认，不得连续推进下一阶段。

## 仿真界面维护规则

1. 可以建立 `MockWindow`、`MockSidebar`、`MockField`、`MockStatusBadge`、
   `MockActionBar` 等浅层基础组件。
2. 不使用复杂配置 JSON 描述整张 3X-UI、Clash Verge Rev 或 Shadowrocket
   界面。
3. MDX 中的组件调用必须保持可读。
4. 只使用一次且结构特殊的界面可以保留为专用组件。
5. 优先保证非程序员能够直接找到并修改界面文字。

## 内容迁移台账

第二册迁移必须同步维护 `docs/migration/book02-content-map.md`，至少记录：

- 原型锚点或区块名称
- 目标章节文件
- 目标组件
- 迁移状态
- 是否发生文字修改
- 修改原因

数量检查不能替代逐段人工复核。正文原则上不主动改写；任何删减、合并或重写都必须
在台账中标记。

## 技术事实处理

迁移阶段只直接修正明显错误、失效链接、官方命令变化和高风险安全表述。其他可能变化
的事实应标记到迁移报告中，提出建议并等待后续内容校订阶段处理。
