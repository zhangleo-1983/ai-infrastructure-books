---
interface Props {
  chapterId: string;
  items?: string[];
}

const { chapterId, items } = Astro.props;
const messageId = `${chapterId}-status`;
---

<div
  class="chapter-checklist"
  data-chapter-checklist
  data-pagefind-ignore
>
  {
    items && (
      <ul class="chapter-checklist__items">
        {items.map((item) => <li>{item}</li>)}
      </ul>
    )
  }
  <label>
    <input
      type="checkbox"
      name={chapterId}
      value="complete"
      aria-describedby={messageId}
      data-chapter-complete-control
    />
    <span>
      {items ? "以上检查均已完成" : <slot />}
    </span>
  </label>
  <small
    id={messageId}
    data-chapter-status-message
    aria-live="polite"
  ></small>
</div>

<style>
  .chapter-checklist {
    border: 1px dashed var(--color-primary);
    border-radius: var(--radius-lg);
    background: var(--color-primary-soft);
    padding: var(--space-5);
  }

  label {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    cursor: pointer;
    font-weight: 680;
  }

  .chapter-checklist__items {
    margin: 0 0 var(--space-4);
    padding-left: 1.4em;
  }

  .chapter-checklist__items li + li {
    margin-top: var(--space-2);
  }

  input {
    width: 1.125rem;
    height: 1.125rem;
    flex: 0 0 auto;
    margin-top: 0.32rem;
    accent-color: var(--color-primary);
  }

  small {
    display: block;
    margin: var(--space-2) 0 0 1.875rem;
    color: var(--color-text-muted);
    font-size: var(--font-size-xs);
  }

  [data-completed="true"] {
    border-style: solid;
    border-color: var(--color-success);
    background: var(--color-success-soft);
  }

  @media print {
    .chapter-checklist {
      border-color: #888888;
      background: white;
    }

    .chapter-checklist__items li:nth-last-child(-n + 2) {
      break-after: avoid;
    }

    input {
      display: none;
    }

    small {
      display: none;
    }

    label::before {
      content: "□";
      margin-right: 0.25em;
    }
  }
</style>
